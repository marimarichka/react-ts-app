export type ITodoItem = { id: string; text: string };
export type IUser = {
  firstName: string;
  lastName: string;
  phoneNumber?: string;
  email?: string;
  company?: string;
  city?: string;
  country?: string;
  avatar?: string;
  status?: string;
  id: string;
};
